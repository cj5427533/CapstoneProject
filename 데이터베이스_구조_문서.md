# 여기몰까 데이터베이스 구조 문서

> **프로젝트명**: 여기몰까 (YeogiMolkka)  
> **데이터베이스**: PostgreSQL (Supabase)  
> **작성일**: 2025년 11월  
> **최종 업데이트**: 2025년 11월  
> **목적**: 온라인 쇼핑몰의 신뢰도를 분석하고 피해 사례를 공유하는 플랫폼

---

## 📋 목차

1. [데이터베이스 개요](#1-데이터베이스-개요)
2. [테이블 구조 개요](#2-테이블-구조-개요)
3. [사용자 및 인증 테이블](#3-사용자-및-인증-테이블)
4. [쇼핑몰 및 신고/평점 테이블](#4-쇼핑몰-및-신고평점-테이블)
5. [SMS 인증 테이블](#5-sms-인증-테이블)
6. [커뮤니티 테이블](#6-커뮤니티-테이블)
7. [AI 분석 및 사업자 등록 테이블](#7-ai-분석-및-사업자-등록-테이블)
8. [테이블 간 관계도](#8-테이블-간-관계도)
9. [주요 인덱스 및 최적화](#9-주요-인덱스-및-최적화)

---

## 1. 데이터베이스 개요

### 1.1 전체 테이블 수
- **총 17개 테이블** (스키마에 정의된 14개 + 코드에서 사용하는 3개)

### 1.2 테이블 분류

| 카테고리 | 테이블 수 | 설명 |
|---------|----------|------|
| 사용자 및 인증 | 3개 | 사용자 정보, 비밀번호 재설정, 로그인 로그 |
| 쇼핑몰 및 신고/평점 | 6개 | 쇼핑몰 정보, 검색 로그, 신고, 평점, 업로드 파일 |
| SMS 인증 | 3개 | SMS 인증, 요청 추적, 요청 로그 |
| 커뮤니티 | 3개 | 게시글, 댓글, 좋아요 |
| AI 분석 및 사업자 등록 | 5개 | 신뢰도 점수, ML 예측 결과, AI 분석 캐시, 웹 분석, 사업자 정보 |

---

## 2. 테이블 구조 개요

### 2.1 테이블 목록

#### 사용자 및 인증 (3개)
1. `users` - 사용자 정보
2. `password_reset_tokens` - 비밀번호 재설정 토큰
3. `user_login_logs` - 사용자 로그인 로그

#### 쇼핑몰 및 신고/평점 (6개)
4. `shops` - 쇼핑몰 정보
5. `shop_search_logs` - 쇼핑몰 검색 로그 (코드에서 사용, 스키마에는 별도 관리)
6. `reports` - 신고 내역
7. `ratings` - 평점 및 리뷰
8. `uploaded_files` - 신고 증빙 파일

#### SMS 인증 (3개)
9. `sms_verifications` - SMS 인증 정보
10. `sms_request_tracking` - SMS 요청 추적
11. `sms_request_logs` - SMS 요청 로그 (코드에서 사용, 레이트 리밋용)

#### 커뮤니티 (3개)
12. `community_posts` - 커뮤니티 게시글
13. `community_comments` - 커뮤니티 댓글
14. `community_post_likes` - 게시글 좋아요

#### AI 분석 및 사업자 등록 (5개)
15. `shop_trust_scores` - 쇼핑몰 신뢰도 점수 (코드에서 사용, 1:1 관계)
16. `ml_prediction_results` - ML 피싱 예측 결과 (코드에서 사용, URL 기반)
17. `ai_analysis_cache` - AI 분석 결과 캐시
18. `web_analysis` - 웹 분석 결과
19. `business_registrations` - 사업자 등록 정보

---

## 3. 사용자 및 인증 테이블

### 3.1 users (사용자 테이블)
**역할**: 회원가입한 사용자의 기본 정보를 저장하는 핵심 테이블

| 컬럼명 | 타입 | 제약조건 | 설명 |
|--------|------|----------|------|
| `id` | BIGINT | PRIMARY KEY | 사용자 고유 ID (자동 증가) |
| `username` | VARCHAR | UNIQUE, NOT NULL | 사용자 이름 (고유값) |
| `email` | VARCHAR | UNIQUE, NOT NULL | 이메일 주소 (고유값) |
| `password` | VARCHAR | NOT NULL | 암호화된 비밀번호 (SHA-256) |
| `phone_number` | VARCHAR | UNIQUE, NOT NULL | 전화번호 (고유값) |
| `created_at` | TIMESTAMP | - | 계정 생성 일시 |
| `updated_at` | TIMESTAMP | - | 정보 수정 일시 |
| `last_login_at` | TIMESTAMP | - | 마지막 로그인 일시 |

**주요 특징**:
- 모든 사용자 정보는 고유값으로 중복 방지
- 비밀번호는 SHA-256으로 해싱되어 저장
- 마지막 로그인 시간 추적 가능

---

### 3.2 password_reset_tokens (비밀번호 재설정 토큰)
**역할**: 비밀번호 재설정 시 사용되는 임시 토큰 관리

| 컬럼명 | 타입 | 제약조건 | 설명 |
|--------|------|----------|------|
| `id` | BIGINT | PRIMARY KEY | 토큰 고유 ID |
| `user_id` | BIGINT | FOREIGN KEY → users.id, NOT NULL | 사용자 ID |
| `token` | VARCHAR | UNIQUE, NOT NULL | 재설정 토큰 (고유값) |
| `expires_at` | TIMESTAMP | NOT NULL | 토큰 만료 시간 |
| `used` | BOOLEAN | - | 사용 여부 (기본값: false) |
| `created_at` | TIMESTAMP | - | 토큰 생성 일시 |

**주요 특징**:
- 토큰은 일회용이며 만료 시간이 있음
- 사용된 토큰은 재사용 불가

---

### 3.3 user_login_logs (사용자 로그인 로그)
**역할**: 사용자의 로그인 시도 및 성공/실패 기록

| 컬럼명 | 타입 | 제약조건 | 설명 |
|--------|------|----------|------|
| `id` | BIGINT | PRIMARY KEY | 로그 고유 ID |
| `user_id` | BIGINT | FOREIGN KEY → users.id, NOT NULL | 사용자 ID |
| `ip_address` | VARCHAR | - | 로그인 시도 IP 주소 |
| `user_agent` | VARCHAR | - | 사용자 브라우저 정보 |
| `login_success` | BOOLEAN | - | 로그인 성공 여부 |
| `failure_reason` | VARCHAR | - | 실패 사유 (INVALID_PASSWORD, USER_NOT_FOUND, ACCOUNT_DELETED 등) |
| `created_at` | TIMESTAMP | - | 로그인 시도 일시 |

**주요 특징**:
- 모든 로그인 시도 기록 (성공/실패 모두)
- 보안 모니터링 및 이상 징후 탐지에 활용

---


## 4. 쇼핑몰 및 신고/평점 테이블

### 4.1 shops (쇼핑몰 테이블)
**역할**: 분석 대상 쇼핑몰의 기본 정보 저장

| 컬럼명 | 타입 | 제약조건 | 설명 |
|--------|------|----------|------|
| `id` | BIGINT | PRIMARY KEY | 쇼핑몰 고유 ID |
| `url` | VARCHAR(500) | UNIQUE, NOT NULL | 정규화된 쇼핑몰 URL (고유값) |
| `name` | VARCHAR(255) | - | 쇼핑몰 이름 (웹 스크래핑으로 자동 추출) |
| `parent_shop_id` | BIGINT | FOREIGN KEY → shops.id | 부모 쇼핑몰 ID (자기 참조, 쇼핑몰 병합용) |
| `search_count` | INTEGER | DEFAULT 0 | 검색 횟수 (인기도 측정용) |
| `created_at` | TIMESTAMP WITH TIME ZONE | DEFAULT NOW() | 생성 일시 |
| `updated_at` | TIMESTAMP WITH TIME ZONE | DEFAULT NOW() | 수정 일시 |

**주요 특징**:
- URL은 정규화되어 저장 (www 제거, 프로토콜 통일 등)
- 자기 참조를 통한 쇼핑몰 병합 기능 지원
- 검색 횟수 추적으로 인기 쇼핑몰 파악 가능

---

### 4.2 shop_search_logs (쇼핑몰 검색 로그)
**역할**: 사용자가 쇼핑몰을 검색한 이력 기록

| 컬럼명 | 타입 | 제약조건 | 설명 |
|--------|------|----------|------|
| `id` | BIGINT | PRIMARY KEY | 로그 고유 ID |
| `normalized_url` | VARCHAR | NOT NULL | 정규화된 URL |
| `raw_url` | TEXT | - | 원본 URL (사용자가 입력한 그대로) |
| `user_id` | BIGINT | FOREIGN KEY → users.id | 검색한 사용자 ID (비회원은 NULL) |
| `ip_address` | VARCHAR | - | 검색한 IP 주소 |
| `user_agent` | TEXT | - | 사용자 브라우저 정보 |
| `created_at` | TIMESTAMP | NOT NULL | 검색 일시 |

**주요 특징**:
- 비회원 검색도 기록 가능 (user_id가 NULL)
- 원본 URL과 정규화된 URL 모두 저장하여 분석 가능

---

### 4.3 reports (신고 테이블)
**역할**: 사용자가 쇼핑몰에 대한 피해 신고 내역 저장

| 컬럼명 | 타입 | 제약조건 | 설명 |
|--------|------|----------|------|
| `id` | BIGINT | PRIMARY KEY | 신고 고유 ID |
| `shop_id` | BIGINT | FOREIGN KEY → shops.id, NOT NULL | 신고 대상 쇼핑몰 ID |
| `user_id` | BIGINT | FOREIGN KEY → users.id | 신고한 사용자 ID (비회원은 NULL) |
| `reporter_name` | VARCHAR(255) | - | 신고자 이름 |
| `reporter_phone` | VARCHAR(20) | - | 신고자 전화번호 |
| `categories` | TEXT | NOT NULL | 신고 카테고리 (JSON 배열 형식) |
| `description` | TEXT | NOT NULL | 신고 내용 설명 |
| `evidence_type` | VARCHAR(50) | DEFAULT 'NONE' | 증빙 자료 유형 (NONE, IMAGE, VIDEO, DOCUMENT 등) |
| `evidence_files` | TEXT | - | 증빙 파일 경로 (JSON 배열 형식) |
| `evidence_verified` | BOOLEAN | DEFAULT FALSE | 증빙 자료 검증 여부 |
| `verification_score` | INTEGER | DEFAULT 0 | 검증 점수 (0-100) |
| `report_type` | VARCHAR(50) | DEFAULT 'GENERAL_REVIEW' | 신고 유형 (GENERAL_REVIEW, FRAUD, DELAYED_DELIVERY 등) |
| `status` | VARCHAR(20) | DEFAULT 'pending' | 신고 승인 상태 (pending, approved, rejected) |
| `created_at` | TIMESTAMP WITH TIME ZONE | DEFAULT NOW() | 신고 작성 일시 |
| `updated_at` | TIMESTAMP WITH TIME ZONE | DEFAULT NOW() | 신고 수정 일시 |

**주요 특징**:
- 회원/비회원 모두 신고 가능 (user_id 선택적)
- 증빙 파일은 JSON 배열로 여러 개 저장 가능
- 관리자가 검증 및 점수 부여 가능

---

### 4.4 ratings (평점 및 리뷰 테이블)
**역할**: 사용자가 쇼핑몰에 부여한 평점 및 리뷰 저장

| 컬럼명 | 타입 | 제약조건 | 설명 |
|--------|------|----------|------|
| `id` | BIGINT | PRIMARY KEY | 평점 고유 ID |
| `shop_id` | BIGINT | FOREIGN KEY → shops.id, NOT NULL | 평점 대상 쇼핑몰 ID |
| `user_id` | BIGINT | FOREIGN KEY → users.id | 평점 작성 사용자 ID (비회원은 NULL) |
| `rating` | INTEGER | NOT NULL | 평점 (1-5점, CHECK 제약) |
| `comment` | TEXT | - | 리뷰 내용 (선택적) |
| `created_at` | TIMESTAMP | - | 평점 작성 일시 |

**주요 특징**:
- 평점은 필수, 리뷰(comment)는 선택적
- 회원/비회원 모두 평점 작성 가능
- 평점은 1-5점 범위로 제한

---

### 4.5 uploaded_files (신고 증빙 파일)
**역할**: 신고 시 업로드된 증빙 파일의 메타데이터 저장

| 컬럼명 | 타입 | 제약조건 | 설명 |
|--------|------|----------|------|
| `id` | BIGINT | PRIMARY KEY | 파일 고유 ID |
| `report_id` | BIGINT | FOREIGN KEY → reports.id, NOT NULL | 관련 신고 ID |
| `file_name` | VARCHAR | NOT NULL | 원본 파일명 |
| `file_path` | VARCHAR | NOT NULL | 서버에 저장된 파일 경로 |
| `file_size` | BIGINT | NOT NULL | 파일 크기 (바이트) |
| `file_type` | VARCHAR | NOT NULL | 파일 타입 (MIME 타입) |
| `upload_date` | TIMESTAMP | - | 업로드 일시 |
| `verification_status` | VARCHAR | - | 검증 상태 (PENDING, VERIFIED, REJECTED) |

**주요 특징**:
- 파일은 물리적으로 `backend/uploads/` 디렉토리에 저장
- 메타데이터만 데이터베이스에 저장
- 관리자가 검증 상태 관리 가능

---

## 5. SMS 인증 테이블

### 5.1 sms_verifications (SMS 인증 정보)
**역할**: SMS 인증번호 발송 및 검증 정보 저장

| 컬럼명 | 타입 | 제약조건 | 설명 |
|--------|------|----------|------|
| `id` | BIGINT | PRIMARY KEY | 인증 고유 ID |
| `phone_number` | VARCHAR | NOT NULL | 인증 대상 전화번호 |
| `verification_code` | VARCHAR | NOT NULL | 6자리 인증번호 |
| `is_verified` | BOOLEAN | - | 인증 완료 여부 (기본값: false) |
| `expires_at` | TIMESTAMP | - | 인증번호 만료 시간 (3분) |
| `user_id` | BIGINT | FOREIGN KEY → users.id | 관련 사용자 ID (회원가입 시) |
| `sms_request_tracking_id` | BIGINT | FOREIGN KEY → sms_request_tracking.id | SMS 요청 추적 ID |
| `created_at` | TIMESTAMP | - | 인증번호 생성 일시 |

**주요 특징**:
- 인증번호는 6자리 숫자
- 만료 시간은 3분
- SMS 요청 추적과 연결되어 레이트 리밋 관리

---

### 5.2 sms_request_tracking (SMS 요청 추적)
**역할**: SMS 발송 요청 추적 및 레이트 리밋 관리

| 컬럼명 | 타입 | 제약조건 | 설명 |
|--------|------|----------|------|
| `id` | BIGINT | PRIMARY KEY | 추적 고유 ID |
| `phone_number` | VARCHAR(20) | NOT NULL | 요청한 전화번호 |
| `ip_address` | INET | - | 요청 IP 주소 (IPv4/IPv6) |
| `sent_count` | INTEGER | DEFAULT 1 | 발송 횟수 (분당 제한용) |
| `last_sent_at` | TIMESTAMP WITH TIME ZONE | DEFAULT NOW() | 마지막 발송 시간 |
| `user_id` | BIGINT | FOREIGN KEY → users.id | 관련 사용자 ID (선택적) |
| `created_at` | TIMESTAMP WITH TIME ZONE | DEFAULT NOW() | 첫 요청 일시 |
| `updated_at` | TIMESTAMP WITH TIME ZONE | DEFAULT NOW() | 마지막 업데이트 일시 |

**주요 특징**:
- 전화번호 및 IP 주소별로 레이트 리밋 관리
- 1분당 5회 제한으로 스팸 방지
- SMS 인증과 연결되어 추적 가능

---

### 5.3 sms_request_logs (SMS 요청 로그)
**역할**: SMS 요청 로그 저장 (sms_request_tracking과 별도, 레이트 리밋용)

| 컬럼명 | 타입 | 제약조건 | 설명 |
|--------|------|----------|------|
| `id` | BIGINT | PRIMARY KEY | 로그 고유 ID |
| `phone_number` | VARCHAR(20) | NOT NULL | 요청 전화번호 |
| `ip_address` | INET | - | 요청 IP 주소 (IPv4/IPv6) |
| `sent_count` | INTEGER | DEFAULT 1 | 발송 횟수 (분당 제한용) |
| `last_sent_at` | TIMESTAMP WITH TIME ZONE | DEFAULT NOW() | 마지막 발송 시간 |
| `updated_at` | TIMESTAMP WITH TIME ZONE | DEFAULT NOW() | 정보 수정일시 |

**주요 특징**:
- sms_request_tracking과 별도로 관리되는 레이트 리밋용 테이블
- 코드에서 실제 사용 중 (smsService.js)

---

## 6. 커뮤니티 테이블

### 6.1 community_posts (커뮤니티 게시글)
**역할**: 사용자가 작성한 커뮤니티 게시글 저장

| 컬럼명 | 타입 | 제약조건 | 설명 |
|--------|------|----------|------|
| `id` | BIGINT | PRIMARY KEY | 게시글 고유 ID |
| `user_id` | BIGINT | FOREIGN KEY → users.id, NOT NULL | 작성자 ID |
| `title` | VARCHAR | NOT NULL | 게시글 제목 |
| `content` | TEXT | NOT NULL | 게시글 내용 |
| `views` | INTEGER | - | 조회수 (기본값: 0) |
| `likes` | INTEGER | - | 좋아요 수 (기본값: 0) |
| `created_at` | TIMESTAMP | - | 작성 일시 |
| `updated_at` | TIMESTAMP | - | 수정 일시 |

**주요 특징**:
- 회원만 게시글 작성 가능 (user_id 필수)
- 조회수와 좋아요 수 자동 집계

---

### 6.2 community_comments (커뮤니티 댓글)
**역할**: 게시글에 대한 댓글 저장

| 컬럼명 | 타입 | 제약조건 | 설명 |
|--------|------|----------|------|
| `id` | BIGINT | PRIMARY KEY | 댓글 고유 ID |
| `post_id` | BIGINT | FOREIGN KEY → community_posts.id, NOT NULL | 게시글 ID |
| `user_id` | BIGINT | FOREIGN KEY → users.id, NOT NULL | 작성자 ID |
| `content` | VARCHAR | NOT NULL | 댓글 내용 (최대 500자) |
| `created_at` | TIMESTAMP | - | 작성 일시 |

**주요 특징**:
- 회원만 댓글 작성 가능
- 댓글은 수정 불가 (삭제만 가능)

---

### 6.3 community_post_likes (게시글 좋아요)
**역할**: 게시글에 대한 좋아요 정보 저장 (중복 방지)

| 컬럼명 | 타입 | 제약조건 | 설명 |
|--------|------|----------|------|
| `id` | BIGINT | PRIMARY KEY | 좋아요 고유 ID |
| `post_id` | BIGINT | FOREIGN KEY → community_posts.id, NOT NULL | 게시글 ID |
| `user_id` | BIGINT | FOREIGN KEY → users.id, NOT NULL | 좋아요 누른 사용자 ID |
| `created_at` | TIMESTAMP | - | 좋아요 일시 |
| UNIQUE | (post_id, user_id) | - | 같은 사용자가 같은 게시글에 중복 좋아요 방지 |

**주요 특징**:
- UNIQUE 제약으로 중복 좋아요 방지
- 좋아요 취소 시 레코드 삭제

---

## 7. AI 분석 및 사업자 등록 테이블

### 7.1 ai_analysis_cache (AI 분석 결과 캐시)
**역할**: AI 분석 결과를 캐싱하여 성능 최적화

| 컬럼명 | 타입 | 제약조건 | 설명 |
|--------|------|----------|------|
| `id` | BIGINT | PRIMARY KEY | 캐시 고유 ID |
| `shop_id` | BIGINT | FOREIGN KEY → shops.id, NOT NULL | 분석 대상 쇼핑몰 ID |
| `analysis_type` | VARCHAR | NOT NULL | 분석 유형 (RISK_ANALYSIS, BUSINESS_ANALYSIS, WEB_ANALYSIS 등) |
| `analysis_result` | TEXT | NOT NULL | 분석 결과 (JSON 형태) |
| `analysis_date` | TIMESTAMP | - | 분석 일시 |
| `expires_at` | TIMESTAMP | NOT NULL | 캐시 만료 시간 |
| `created_at` | TIMESTAMP | - | 캐시 생성 일시 |

**주요 특징**:
- AI 분석 비용 절감을 위한 캐싱
- 만료 시간이 지나면 재분석 필요
- 분석 유형별로 별도 캐싱

---

### 7.2 web_analysis (웹 분석 결과)
**역할**: 웹 크롤링 및 기술적 분석 결과 저장

| 컬럼명 | 타입 | 제약조건 | 설명 |
|--------|------|----------|------|
| `id` | BIGINT | PRIMARY KEY | 분석 고유 ID |
| `shop_id` | BIGINT | FOREIGN KEY → shops.id, NOT NULL | 분석 대상 쇼핑몰 ID |
| `suspicious_keywords` | TEXT | - | 의심스러운 키워드 (JSON 형태) |
| `price_analysis` | TEXT | - | 가격 분석 결과 (JSON 형태) |
| `technical_analysis` | TEXT | - | 기술적 분석 결과 (JSON 형태) |
| `domain_analysis` | TEXT | - | 도메인 분석 결과 (JSON 형태) |
| `analysis_date` | TIMESTAMP | - | 분석 일시 |
| `analysis_source` | VARCHAR | - | 분석 출처 (기본값: AUTO, AUTO/MANUAL) |
| `confidence_score` | INTEGER | - | 신뢰도 점수 (0-100, 기본값: 0) |
| `created_at` | TIMESTAMP | - | 분석 생성 일시 |

**주요 특징**:
- 자동 분석 및 수동 분석 모두 지원
- 다양한 분석 결과를 JSON 형태로 저장
- 신뢰도 점수로 분석 품질 평가

---

### 7.3 shop_trust_scores (쇼핑몰 신뢰도 점수)
**역할**: 쇼핑몰 신뢰도 점수 저장 (1:1 관계)

| 컬럼명 | 타입 | 제약조건 | 설명 |
|--------|------|----------|------|
| `id` | BIGINT | PRIMARY KEY | 점수 고유 ID |
| `shop_id` | BIGINT | FOREIGN KEY → shops.id, UNIQUE, NOT NULL | 쇼핑몰 ID (1:1 관계) |
| `tech_risk` | DECIMAL(3,2) | DEFAULT 0.0, CHECK (0~1) | 기술적 위험도 (0~1) |
| `review_risk` | DECIMAL(3,2) | DEFAULT 0.0, CHECK (0~1) | 리뷰 위험도 (0~1) |
| `report_penalty` | INTEGER | DEFAULT 0, CHECK (0~15) | 신고사례 패널티 (0~15) |
| `final_trust` | INTEGER | DEFAULT 0, CHECK (0~100) | 최종 신뢰도 점수 (0~100) |
| `trust_grade` | VARCHAR(20) | DEFAULT 'LOW' | 신뢰도 등급 (VERY_HIGH, HIGH, CAUTION, LOW, VERY_LOW) |
| `model_version` | VARCHAR(50) | DEFAULT 'v1.0' | 모델 버전 |
| `analyzed_at` | TIMESTAMP WITH TIME ZONE | DEFAULT NOW() | 분석 일시 |
| `created_at` | TIMESTAMP WITH TIME ZONE | DEFAULT NOW() | 생성 일시 |
| `updated_at` | TIMESTAMP WITH TIME ZONE | DEFAULT NOW() | 수정 일시 |

**주요 특징**:
- 쇼핑몰당 1개의 신뢰도 점수만 존재 (1:1 관계)
- 코드에서 trustScoreService.js에서 사용

---

### 7.4 ml_prediction_results (ML 피싱 예측 결과)
**역할**: ML 피싱 예측 결과 저장 (URL 기반, shops와 직접 FK 관계 없음)

| 컬럼명 | 타입 | 제약조건 | 설명 |
|--------|------|----------|------|
| `id` | BIGINT | PRIMARY KEY | 예측 결과 고유 ID |
| `url` | TEXT | NOT NULL | 원본 URL |
| `normalized_url` | TEXT | NOT NULL | 정규화된 URL |
| `label` | INTEGER | NOT NULL, CHECK (0, 1) | 예측 레이블 (0: SAFE, 1: PHISHING) |
| `confidence` | DECIMAL(5,4) | DEFAULT 0.0, CHECK (0~1) | 예측 신뢰도 (0~1) |
| `requested_by` | TEXT | - | 요청자 (IP 주소 등) |
| `source` | VARCHAR(50) | DEFAULT 'API' | 요청 소스 (API, BATCH 등) |
| `created_at` | TIMESTAMP WITH TIME ZONE | DEFAULT NOW() | 생성 일시 |

**주요 특징**:
- shops와 직접 FK 관계 없음 (URL 기반 매칭)
- 코드에서 aiController.js에서 사용

---

### 7.5 business_registrations (사업자 등록 정보)
**역할**: 쇼핑몰의 사업자 등록 정보 저장

| 컬럼명 | 타입 | 제약조건 | 설명 |
|--------|------|----------|------|
| `id` | BIGINT | PRIMARY KEY | 등록 정보 고유 ID |
| `shop_id` | BIGINT | FOREIGN KEY → shops.id, NOT NULL | 쇼핑몰 ID |
| `business_number` | VARCHAR(20) | - | 사업자 등록번호 |
| `registration_date` | DATE | - | 등록일 |
| `business_status` | VARCHAR(20) | DEFAULT 'UNKNOWN' | 사업 상태 (ACTIVE, SUSPENDED, CLOSED, UNKNOWN) |
| `business_type` | VARCHAR(100) | - | 업종 |
| `capital_amount` | BIGINT | - | 자본금 |
| `representative_name` | VARCHAR(100) | - | 대표자명 |
| `business_address` | TEXT | - | 사업장 주소 |
| `phone_number` | VARCHAR(20) | - | 전화번호 |
| `email` | VARCHAR(255) | - | 이메일 |
| `last_verified` | TIMESTAMP WITH TIME ZONE | DEFAULT NOW() | 마지막 검증 일시 |
| `verification_source` | VARCHAR(50) | DEFAULT 'MANUAL' | 검증 출처 (API, MANUAL, CRAWLING) |
| `created_at` | TIMESTAMP WITH TIME ZONE | DEFAULT NOW() | 생성 일시 |
| `updated_at` | TIMESTAMP WITH TIME ZONE | DEFAULT NOW() | 수정 일시 |

**주요 특징**:
- 외부 API, 수동 입력, 크롤링 등 다양한 출처 지원
- 사업자 정보 검증 및 업데이트 추적
- ON CONFLICT (shop_id) DO UPDATE로 UPSERT 지원

---

## 8. 테이블 간 관계도

### 8.1 사용자 관련 관계

```
users (1) ──→ (N) password_reset_tokens
users (1) ──→ (N) user_login_logs
users (1) ──→ (N) shop_search_logs (선택적, 비회원은 NULL)
users (1) ──→ (N) reports (선택적, 비회원은 NULL)
users (1) ──→ (N) ratings (선택적, 비회원은 NULL)
users (1) ──→ (N) community_posts
users (1) ──→ (N) community_comments
users (1) ──→ (N) community_post_likes
users (1) ──→ (N) sms_verifications (선택적)
users (1) ──→ (N) sms_request_tracking (선택적)
```

### 8.2 쇼핑몰 관련 관계

```
shops (1) ──→ (N) shops (parent_shop_id, 자기 참조, 쇼핑몰 병합)
shops (1) ──→ (N) shop_search_logs
shops (1) ──→ (N) reports
shops (1) ──→ (N) ratings
shops (1) ──→ (1) shop_trust_scores (1:1 관계, UNIQUE)
shops (1) ──→ (N) ai_analysis_cache
shops (1) ──→ (N) web_analysis
shops (1) ──→ (N) business_registrations
```

### 8.3 신고 관련 관계

```
reports (1) ──→ (N) uploaded_files
```

### 8.4 커뮤니티 관련 관계

```
community_posts (1) ──→ (N) community_comments
community_posts (1) ──→ (N) community_post_likes
```

### 8.5 SMS 관련 관계

```
sms_request_tracking (1) ──→ (N) sms_verifications
```

**참고**: `sms_request_logs`는 독립적인 테이블로, 다른 테이블과 FK 관계 없음

---

## 9. 주요 인덱스 및 최적화

### 9.1 주요 인덱스 (총 35개)

#### 사용자 테이블 인덱스 (3개)
- `idx_users_email` (email)
- `idx_users_phone_number` (phone_number)
- `idx_users_created_at` (created_at)

#### 쇼핑몰 테이블 인덱스 (3개)
- `idx_shops_url` (url)
- `idx_shops_parent_shop_id` (parent_shop_id)
- `idx_shops_created_at` (created_at)
- ~~`idx_shops_search_count`~~ 제거됨 (사용 빈도 낮음)

#### 신고 테이블 인덱스 (3개)
- `idx_reports_shop_id` (shop_id)
- `idx_reports_user_id` (user_id)
- `idx_reports_created_at` (created_at)
- ~~`idx_reports_evidence_type`~~ 제거됨 (카디널리티 낮음)
- ~~`idx_reports_report_type`~~ 제거됨 (카디널리티 낮음)
- ~~`idx_reports_verification_score`~~ 제거됨 (사용 빈도 낮음)

#### 평점 테이블 인덱스 (3개)
- `idx_ratings_shop_id` (shop_id)
- `idx_ratings_user_id` (user_id)
- `idx_ratings_created_at` (created_at)
- ~~`idx_ratings_rating`~~ 제거됨 (카디널리티 낮음)

#### SMS 테이블 인덱스 (4개)
- `idx_sms_verifications_phone` (phone_number)
- `idx_sms_verifications_code` (verification_code)
- `idx_sms_request_tracking_phone` (phone_number)
- `idx_sms_request_tracking_ip` (ip_address)

#### 비밀번호 재설정 토큰 인덱스 (3개)
- `idx_password_reset_tokens_token` (token)
- `idx_password_reset_tokens_user_id` (user_id)
- `idx_password_reset_tokens_expires_at` (expires_at)

#### 사용자 로그인 기록 인덱스 (3개)
- `idx_user_login_logs_user_id` (user_id)
- `idx_user_login_logs_created_at` (created_at)
- `idx_user_login_logs_ip_address` (ip_address)
- ~~`idx_user_login_logs_login_success`~~ 제거됨 (카디널리티 매우 낮음)

#### 커뮤니티 테이블 인덱스 (6개)
- `idx_community_posts_user_id` (user_id)
- `idx_community_posts_created_at` (created_at)
- `idx_community_comments_post_id` (post_id)
- `idx_community_comments_user_id` (user_id)
- `idx_community_post_likes_post_id` (post_id)
- `idx_community_post_likes_user_id` (user_id)

#### AI 분석 테이블 인덱스 (4개)
- `idx_ai_analysis_cache_shop_id` (shop_id)
- `idx_ai_analysis_cache_expires_at` (expires_at)
- ~~`idx_ai_analysis_cache_type`~~ 제거됨 (사용 빈도 낮음)
- `idx_web_analysis_shop_id` (shop_id)
- ~~`idx_web_analysis_analysis_date`~~ 제거됨 (사용 빈도 낮음)
- `idx_business_registrations_shop_id` (shop_id)
- `idx_business_registrations_business_number` (business_number)
- ~~`idx_business_registrations_business_status`~~ 제거됨 (카디널리티 낮음)
- `idx_uploaded_files_report_id` (report_id)

**인덱스 최적화**: 2025년 1월에 불필요한 인덱스 8개 제거 (카디널리티 낮은 인덱스, 사용 빈도 낮은 인덱스)

### 9.2 성능 최적화 전략

1. **캐싱**: AI 분석 결과를 `ai_analysis_cache` 테이블에 저장하여 중복 분석 방지
2. **검색 로그**: `shop_search_logs` 테이블로 검색 이력 추적 및 분석
3. **인덱싱**: 자주 조회되는 컬럼에 인덱스 추가로 조회 성능 향상 (총 35개 인덱스)
4. **인덱스 최적화**: 카디널리티가 낮거나 사용 빈도가 낮은 인덱스 제거로 메모리 사용량 감소 및 쓰기 성능 향상
5. **외래 키 제약**: 데이터 무결성 보장 (ON DELETE CASCADE, ON DELETE SET NULL)
6. **UNIQUE 제약**: 중복 방지 및 UPSERT 지원 (business_registrations.shop_id, shop_trust_scores.shop_id)

---

## 📝 참고 사항

### 데이터베이스 관리
- **데이터베이스**: Supabase (PostgreSQL)
- **마이그레이션**: `backend/migrations/` 디렉토리에 SQL 파일로 관리
- **ERD 다이어그램**: `ERD.mermaid` 파일 참조

### 주요 제약조건
- **ON DELETE CASCADE**: 필수 관계에서 사용 (reports, ratings, community_posts 등)
- **ON DELETE SET NULL**: 선택적 관계에서 사용 (reports.user_id, ratings.user_id, shops.parent_shop_id 등)
- **UNIQUE 제약**: 중복 방지 (username, email, phone_number, url, shop_trust_scores.shop_id 등)
- **CHECK 제약**: 데이터 유효성 검증 (rating 1-5점, parent_shop_id != id 등)
- **순환 참조 방지**: CHECK 제약조건 + 트리거 함수로 shops.parent_shop_id 순환 참조 방지

### 보안 고려사항
- 비밀번호는 SHA-256으로 해싱
- SMS 인증번호는 3분 만료
- 레이트 리밋으로 스팸 방지
- IP 차단 기능으로 악의적 활동 차단
- 로그인 로그로 보안 모니터링

---

**문서 작성일**: 2025년 1월  
**최종 업데이트**: 2025년 1월  
**버전**: 2.0.0

---

## 변경 이력

### v2.0.0 (2025년 1월)
- 인덱스 최적화: 불필요한 인덱스 8개 제거
- 테이블 구조 업데이트: 실제 스키마에 맞게 수정
- shop_trust_scores, ml_prediction_results, sms_request_logs 테이블 추가
- reports.status 컬럼 추가
- security_alerts, ip_blacklist, user_activity_summary 테이블 제거 (실제 스키마에 없음)
- shops 테이블에서 created_by_user_id, created_via 컬럼 제거 (실제 스키마에 없음)

